import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '80%',
    marginTop: 2,
  },
  countdownLabelSection: {
    marginTop: 8,
    alignSelf: 'center',
  },
});

export default styles;
